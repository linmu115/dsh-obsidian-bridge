> 说明（2026-09-21）：本批过程已整理为可展开来源索引的正式历程 [[HIST-2026-09-20-independent-bridge-release]]（正文在 records/history/，索引在 history/）。本文件仅保留当时的草稿原文，不再是主要入口。

# 独立引用接收修复历程草稿

用户报告 testvault 引用产生气泡并打开 WebUI，但两个 DSH 网页都没有引用气泡，随后按排查请求重新引用。来源为本任务用户消息，待绑定公开事件索引。

现场队列确认 1 条 queued、没有 claim。独立实例的缺席 Maintenance 路由返回 405，旧代码只处理 404。补充真实接收处理器回归，复现能力缺席时仍调用解析器；通过宿主明确声明和客户端分支修复。保留解析失败、逻辑历史身份缺少解析器的保护。17 项测试、类型检查、构建通过。

官方安装 Bridge .7，DSH 独立重启成功，其他插件无回退，Maintenance 仍关闭。Codex 网页刷新成功；Obsidian 内网页需用户刷新，真实引用接收仍待核验。新会话标题与 Companion manifest 版本差异只作为待核查线索，未当成已证实原因。

后续用户反馈“已经出现，会话测试也正常”。只读核验原引用由 queued 转为 claimed，存在同步回执，待接收为 0；Maintenance 端口无监听。本条无引擎引用流程完成用户实际验收。此前“等待刷新”是过程状态，不再是当前阻断；未扩大为其他插件全功能验收。
