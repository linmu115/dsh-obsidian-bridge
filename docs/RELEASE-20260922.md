# 0.4.1-rc2.9 CLI 权限增强

本版让 LLM 通过现有桥读写隐藏配置目录、部署及管理插件、执行任意 JavaScript。两端绑定和持久请求回执沿用；不新增插件依赖，不需要改动 Obsidian 侧桥。

验证：TypeScript、构建通过；30 个测试文件、191 项测试全部通过。包含无 Core/Sticker/Maintenance 的独立加载、公开类型和浏览器边界，隐藏文件与异步脚本、路径越界、重复请求和未知回执。跨仓库测试使用已存在的 Companion 源码位置参数，未新增运行依赖。

真实官方 CLI 已只读核对显式 Vault 路径、config:dir/fs:stat 与编码异步 eval；写入、部署和删除测试均在临时目录配合模拟 Adapter 执行。

发行包仅包含本桥构建、公共类型、bundle patch 和使用文档，去除开发依赖与作者机器路径。实际 tgz 解包后的公开导出、独立 Host/Client 加载及类型检查 2/2 通过。包共 37 文件，附 SHA256SUMS.txt 和逐文件清单。

未进行真实 Vault 写入、插件启停或安装，不替换运行实例。UI、真实模型端到端未验收。
